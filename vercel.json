{
  "version": 2,
  "installCommand": "pnpm install",
  "builds": [
    {
      "src": "artifacts/ndtv-clone/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
